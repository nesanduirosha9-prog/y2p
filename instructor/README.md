
  # Supervisor

  This is a code bundle for Supervisor. The original project is available at https://www.figma.com/design/UzG4vbL5ZfZ4Bs7tvFGywD/Supervisor.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  