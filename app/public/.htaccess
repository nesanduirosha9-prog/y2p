# Front-controller rewrite: send any request that isn't a real file/directory
# to index.php, so the app's own Router can match routes like /login, /timetable.
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
